// Shared types used by both backend and frontend.
// Only export what the FE actually needs to type API responses.
export type { Signal, Horizon, SignalDirection } from './types/signal.js';
export type { PolySignalResult, PolySignalComponents } from './types/polymarket.js';
